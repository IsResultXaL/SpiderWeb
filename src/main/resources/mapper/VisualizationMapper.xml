<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.caogen.spiderWeb.dao.VisualizationDao">

    <select id="getAllUserByJiaYuan" resultType="JiaYuanVisualization">
        SELECT province AS name,SUM(userbyjiayuan.sex="男") AS valueMale ,SUM(userbyjiayuan.sex="女") AS valueFemale FROM userbyjiayuan
        WHERE province IS NOT NULL GROUP BY province
    </select>

</mapper>


